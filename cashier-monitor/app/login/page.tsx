'use client';
import {useState} from 'react';
export default function Login(){const [error,setError]=useState(''),[busy,setBusy]=useState(false);return <main style={{maxWidth:440,margin:'10vh auto',padding:24}}><p className="eyebrow">MAD PARFUMEUR</p><h1>دخول الإدارة</h1><p>مساحة خاصة للمبيعات وتسجيلات الكاشير.</p><form onSubmit={async e=>{e.preventDefault();setBusy(true);setError('');try{const form=new FormData(e.currentTarget);const r=await fetch('/api/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({password:form.get('password')})});const d=await r.json() as {error:string};if(!r.ok)throw Error(d.error);window.location.assign('/');}catch(e){setError((e as Error).message)}finally{setBusy(false)}}}><label>كلمة المرور<input style={{display:'block',width:'100%',padding:12,margin:'12px 0',border:'1px solid #aaa',borderRadius:8}} type="password" name="password" autoComplete="current-password" required maxLength={256}/></label><button disabled={busy} style={{background:'#243c34',color:'white',padding:'12px 28px',borderRadius:8}}>{busy?'جارٍ الدخول…':'دخول'}</button>{error&&<p role="alert">{error}</p>}</form></main>}
